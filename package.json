{
  "name": "intempo",
  "private": true,
  "description": "Repository root. Not a Node project \u2014 this exists so a CI or hosting build that starts at the repo root can reach the app in mobile/.",
  "scripts": {
    "build": "cd mobile && npm ci && npm run build:web"
  }
}
