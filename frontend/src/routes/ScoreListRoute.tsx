import { StubPage } from "../components/StubPage";

export function ScoreListRoute() {
  return (
    <StubPage eyebrow="Your library" title="Scores">
      Your saved pieces will live here. Capture your first one from the home
      screen — the score-library UI arrives in Batch 6.
    </StubPage>
  );
}
