import { StubPage } from "../components/StubPage";

export function ScoreCaptureRoute() {
  return (
    <StubPage eyebrow="New recording" title="Capture a score">
      Photograph a page of sheet music to get started. The camera + OCR
      review flow is built in Batch 6.
    </StubPage>
  );
}
